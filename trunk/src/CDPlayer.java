
public class CDPlayer {
	public int isRandom(String[] songlist, int n){ 
		for(String song:songlist) {
			if (song.length() < n)
				return -1;
			//if (song.contains(s))
		}
		return -1;
	}
}
